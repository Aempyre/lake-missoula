# Specify -dockerless switch if running outside of the Container
# -dockerless alone will still default to range of 100
# -dockerless nn    will request "nn" for the range.
java FizzBuzzClient -dockerless 88